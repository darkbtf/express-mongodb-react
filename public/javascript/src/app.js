
var React = require('react');

var App = React.createClass({
  
  render: function() {
    return (
      <div />
    );
  }
});

React.render(
  <App />,
  document.getElementById('app')
);
//$('.ui.modal').modal('show');
